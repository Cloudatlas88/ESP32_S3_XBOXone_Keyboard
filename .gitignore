# ============================================================
#  ESP32-S3 手柄改键盘项目 —— Git 忽略规则
#  原则：源码 / 配置 / 文档 / 归档清单入库；构建产物与安装包不入库
# ============================================================

# ---- ESP-IDF 构建产物（可随时重新生成）----
**/build/
**/managed_components/
**/dependencies.lock
**/sdkconfig.old
**/compile_commands.json

# ---- .NET 构建产物 ----
**/bin/
**/obj/
**/*.user

# ---- Python 字节码缓存 ----
# 之前 tools/__pycache__/*.pyc 被误入库，每次跑脚本都会产生 diff 噪声
__pycache__/
*.pyc
*.pyo

# ---- 上位机崩溃日志（运行时产物，不入库）----
crash.log

# ---- 工具安装包缓存（约 300 MB，不入库）----
_downloads/

# ---- 安装 / 构建日志 ----
_*.log
*.log

# ---- 编辑器 / 系统 ----
.vscode/*
!.vscode/settings.json
Thumbs.db
desktop.ini
*.swp
*~

# ---- 说明 ----
# sdkconfig 故意【不】忽略：它是复现某次烧录结果所必需的配置快照。
# 每次烧录的固件二进制存放在 flash_archive/ 下，随源码一并入库（单个约 200 KB）。
